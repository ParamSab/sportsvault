import InvitePage from '@/components/InvitePage';

export const metadata = {
    title: 'Invite Friends | SportsVault',
    description: 'Invite your friends to join your games on SportsVault.',
};

export default function Page() {
    return <InvitePage />;
}
